#!/bin/bash

dnf update
dnf install gcc-aarch64-linux-gnu libc6-dev-arm64-cross libblkid-dev kpartx gdisk -y

curl -sL -o ./gimme https://raw.githubusercontent.com/travis-ci/gimme/master/gimme'
chmod +x ./gimme'

GIMME_OUTPUT=$(./gimme ${GOVERSION}) && eval $GIMME_OUTPUT
